{% extends "GestionBundle::layout.html.twig" %} 
{% block titulo %} GESTIÓN DE SOCIOS-CONSULTA{% endblock %}
{% block contenido %}
    <div class="container">
        {% for message in app.session.flashbag().get('status') %}
            <div class="alert alert-success">
                {{message}}
            </div>
        {% endfor %}
        <a href="{{path("addUsuario")}}" class="btn btn-success">Nuevo Socio</a>
        <hr/>
        
        <table class="tabla table" border="1">
            <thead>
                <tr class="success">
                    <th class="text-center text-bold">ID</th>
                    <th class="text-center text-bold">Usuario</th>
                    <th class="text-center text-bold">Nombre</th>
                    <th class="text-center text-bold">Perfil</th>
                    <th class="text-center text-bold">Acciones</th>
                </tr>
            </thead>
            <tbody>
                {% for Socio in SocioALL %}
                    <tr>
                        <td class="text-center">{{socio.id}}</td>
                        <td>{{Socio.ersona}}</td>
                        <td>{{Usuario.nombre}}</td>
                        <td>{{Usuario.perfil}}</td>
                        <td class="text-center">
                           <a href="{{path("editUsuario",{ "id":Usuario.id })}}" class="btn btn-success">Editar</a>
                           <a href="{{path("deleteUsuario",{ "id":Usuario.id })}}" class="btn btn-danger">Eliminar</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}