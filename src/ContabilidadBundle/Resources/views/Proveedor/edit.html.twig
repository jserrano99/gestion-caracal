{% extends "ContabilidadBundle::menu.html.twig" %} 
{% block titulo %} Proveedor: {{Proveedor.descripcion}} 
    <a href="{{path("queryProveedor")}}" class="btn btn-success">Volver</a>
    <hr/>
{% endblock %}
    
{% block contenido %}
    <div class="container">
        {{form_start (form,{'action':'','method':'POST'})}}
        {{form_end (form)}}
    </div>
{% endblock %}